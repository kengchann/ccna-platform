"""Question Bank Import Engine.

Converts source documents (PDF first, other formats later) into canonical
import bundles consumed by the learning platform. See docs/import-engine.md
at the repository root for the architecture and the fidelity rules that bind
every source implementation.
"""

__version__ = "0.1.0"
